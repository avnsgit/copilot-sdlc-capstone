# Step 1: Requirements Agent

1. Read Jira story via Atlassian MCP.
2. Interactively ask the user 3–4 targeted clarification questions.
3. Consolidate responses and write `requirements.md`.
4. Trigger `@confluence-tree-creator` to publish `01-Requirements Spec` under the project root page.