# Step 2: Architecture Agent

1. Read `generatedDocs/requirements.md`.
2. Generate modular component specs and Mermaid flow diagrams. Write to `generatedDocs/architecture.md`.
3. Trigger `@confluence-tree-creator` to publish `02-System Architecture` under the project root page.